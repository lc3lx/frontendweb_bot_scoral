# Production build env for Vite (baked into the JS bundle at build time).
# Copy on VPS:
#   cp .env.production.example .env.production
#
# Same-origin API (Nginx proxies /api → backend): leave empty or omit.
# Direct API URL: set to https://www.scaralphaai.com or your API host.
VITE_API_BASE_URL=
